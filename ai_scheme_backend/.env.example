# PostgreSQL connection string for the existing project database
DATABASE_URL=postgresql+psycopg2://postgres:YOUR_PASSWORD@localhost:5432/YOUR_DATABASE

# React/Vite frontend
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Keep mock until Ananya's real intelligence_engine.py is connected.
# Then change to: real
ENGINE_MODE=mock
